const TaskManager = require("../TaskManager");

/**
 * @description Default TaskManager single node, multi process DockUI instances
 */
class DefaultTaskManager extends TaskManager {
  constructor() {
    super();
  }
}

module.exports = DefaultTaskManager;
