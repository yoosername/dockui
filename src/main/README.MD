# Default setup of DockUI Instance (used by CLI)

## Example

```javascript
const { DockUI } = require("./");
const instance = new DockUI();

// Start it
instance.start();
// Shut it down
instance.shutdown();
```
